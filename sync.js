console.log("Node starting...");
// var greeter = require('./greeter');
var fs = require('fs');
//console.log(fs);

//fs.readFileSync will initially return something as a buffer object.
//Buffer instances are designed to handle raw binary data. Each buffer corresponds to some raw-
//memory allocated outside V8. Each integer in a buffer is actually represented as a byte.
//console.log(fs.readFileSync('./file1.txt', 'utf8');

// greeter.shout("CHRISTIAN");
// greeter.greet("Christian");


